@model ViewModels.WorkOrder.CostPerformance.CostPerformanceViewModel
@{
    Layout = null;
    var totalMaterialCost = Model.TaskMaterials.Sum(x => x.Cost);
}
<h6 class="text-start">Material</h6>
<div class="table-responsive my-3" id="equipmentTable">
    <table class="table table-bordered dataTable">
        <thead>@* class="thead-primary" *@
            <tr>
                <th>Material</th>
                <th>Cost</th>
            </tr>
        </thead>
        <tbody class="task-equipment-tbody">
            @if (Model.TaskMaterials.Count > 0)
            {
                for (var i = 0; i < Model.TaskMaterials.Count(); i++)
                {
                    <tr>
                        <td>
                            <div>@Model.TaskMaterials[i].MaterialName</div>
                        </td>
                        <td>
                            <div>@Model.TaskMaterials[i].Cost.ToString("C")</div>
                        </td>
                    </tr>

                }
            }
            else
            {
                <tr>
                    <td colspan="3" class="text-center">No Records Added.</td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <td colspan="1" class="text-end"><label class="form-label fw-600">Estimated Cost</label></td>
                <td class="text-end"><label class="form-label fw-600">@totalMaterialCost.ToString("C")</label></td>
            </tr>
        </tfoot>
    </table>
</div>