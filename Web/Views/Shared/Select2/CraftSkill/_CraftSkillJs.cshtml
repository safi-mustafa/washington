<script>
    function SetCraftSkillSelect2(identifier) {
        let id = identifier;
        let pageSize = 10;
        let url = '/CraftSkill/Select2';
        let dataArray = function (params) {
            params.page = params.page || 1;
            return {
                prefix: params.term,
                pageSize: pageSize,
                pageNumber: params.page,
            };
        };
        Select2AutoCompleteAjax(id, url, dataArray, pageSize, "Select Craft Skill", 0, false, false, craftSkillAfterSelect);
    }
    function craftSkillAfterSelect(element, event, recordId, modelPropertyName, model) {
    }
</script>